[tool.poetry]
name = "xvalidator"
version = "1.0.0"
description = "A simple Python package for validation"
authors = ["Wation"]

[tool.poetry.dependencies]
python = "^3.6"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"