[tool.poetry]
name = "gitwho"
version = "0.0.0"
description = "A CLI tool to collect names of authors and committers for a Git org."
authors = ["Leron Gray <daddycocoaman@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
