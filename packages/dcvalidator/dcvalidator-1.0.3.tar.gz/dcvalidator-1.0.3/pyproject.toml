[tool.poetry]
name = "dcvalidator"
version = "1.0.3"
description = "A simple Python package designed to showcase Dependency Confusion attacks."
authors = ["Wation"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.6"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"