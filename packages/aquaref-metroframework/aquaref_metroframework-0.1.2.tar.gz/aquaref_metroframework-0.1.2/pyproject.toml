[tool.poetry]
name = "aquaref-metroframework"
version = "0.1.2"
description = "WinForms UI Pack"
authors = ["XiangQinxi <XiangQinxi@outlook.com>"]
readme = "README.md"
packages = [
    { include = "aquaref" },
]

[tool.poetry.dependencies]
aquaref = "^0.1.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
