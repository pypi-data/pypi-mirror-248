[tool.poetry]
name = "finderfilepath"
version = "0.1.2"
description = "Filehound é uma ferramenta de busca de arquivos para sistemas operacionais desenvolvida em Python."
authors = ["Dgeison <dgeison.peixoto@gmail.com>"]
readme = "README.md"
packages = [{include = "file_finder"}]

[tool.poetry.dependencies]
python = "^3.10"
click = "^8.1.7"
tabulate = "^0.9.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
finder = "file_finder.cli:cli"
