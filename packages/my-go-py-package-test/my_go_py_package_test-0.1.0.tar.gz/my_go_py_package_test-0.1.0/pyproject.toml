[tool.poetry]
name = "my-go-py-package-test"
version = "0.1.0"
description = ""
authors = ["wwzip"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
