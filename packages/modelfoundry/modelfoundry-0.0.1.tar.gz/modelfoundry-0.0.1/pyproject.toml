[tool.poetry]
name = "modelfoundry"
version = "0.0.1"
description = ""
authors = ["\"unit_test\" <\"test@unit.com\">"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
