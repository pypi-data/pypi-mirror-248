from setuptools import setup, find_packages

setup(name='test_matstat',
      version='1',
      description='none',
      packages=['test_matstat'],
      author_email='fisefag888@vkr1.com',
      zip_safe=False)
