[tool.poetry]
name = "tunnel-2-by-jumphost"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"
packages = [{include = "tunnel_2_by_jumphost"}]

[tool.poetry.dependencies]
python = "^3.11"
fire = "^0.5.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
my_script = 'tunnel_2_by_jumphost.tunnel_2_by_jumphost'